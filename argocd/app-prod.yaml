# ArgoCD Application for PROD Environment
# This configures ArgoCD to sync backend-a and backend-b deployments to the PROD AKS cluster

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backends-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/stevenfishertc/AshCapstoneAPIApp.git
    targetRevision: HEAD
    path: k8s/overlays/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

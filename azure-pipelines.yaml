trigger:
  branches:
    include:
      - main

variables:
  DEV_ACR_NAME: acrcapstonedeva1b2c
  QA_ACR_NAME: acrcapstoneqaa1b2c
  PROD_ACR_NAME: acrcapstoneproda1b2c
  ACR_LOGIN_SERVER_SUFFIX: .azurecr.io
  IMAGE_TAG: $(Build.BuildId)

  AZURE_SUBSCRIPTION: 'stevens-capstone-conn'

  DEV_RESOURCE_GROUP: steven-rg-capstone-dev
  QA_RESOURCE_GROUP: steven-rg-capstone-qa
  PROD_RESOURCE_GROUP: steven-rg-capstone-prod

  DEV_AKS_NAME: aks-capstone-dev
  QA_AKS_NAME: aks-capstone-qa
  PROD_AKS_NAME: aks-capstone-prod

  K8S_NAMESPACE: capstone-backend

pool:
  vmImage: ubuntu-latest

stages:
# =========================
# 1) BUILD AND PUSH TO DEV
# =========================
- stage: Deploy_Dev
  displayName: Deploy to AKS (DEV)
  jobs:
  - job: DeployDev
    displayName: Deploy backend-a & backend-b to aks-capstone-dev
    steps:
    - checkout: self

    # ---------- LOGIN TO ACR ----------
    - task: AzureCLI@2
      displayName: "Azure Login ACR (DEV)"
      inputs:
        azureSubscription: $(AZURE_SUBSCRIPTION)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az acr login --name $(DEV_ACR_NAME)

    # ---------- BUILD DOCKER ----------
    - script: |
        docker build -f backend-a/Dockerfile \
          -t $(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(Build.BuildId) backend-a

        docker build -f backend-b/Dockerfile \
          -t $(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(Build.BuildId) backend-b
      displayName: Build Images (DEV)

    # ---------- PUSH IMAGES ----------
    - script: |
        docker push $(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG)
        docker push $(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)
      displayName: "Push Images to ACR (DEV)"

    # ---------- DEPLOY TO AKS ----------
    - task: AzureCLI@2
      displayName: Deploy to AKS (DEV)
      inputs:
        azureSubscription: $(AZURE_SUBSCRIPTION)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          set -euo pipefail

          az aks get-credentials \
            --resource-group $(DEV_RESOURCE_GROUP) \
            --name $(DEV_AKS_NAME) \
            --admin \
            --overwrite-existing

          cd k8s/overlays/dev
          
          kustomize edit set image backend-a=$(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG) backend-b=$(DEV_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)
          
          kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.9.6/deploy/static/provider/cloud/deploy.yaml

          # Patch ingress controller to use LoadBalancer instead of NodePort
          kubectl patch svc ingress-nginx-controller -n ingress-nginx -p '{"spec":{"type":"LoadBalancer"}}'

          # Wait for LoadBalancer IP to be assigned
          echo "Waiting for ingress LoadBalancer IP..."
          for i in {1..60}; do
            INGRESS_IP=$(kubectl get svc ingress-nginx-controller -n ingress-nginx -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
            if [ ! -z "$INGRESS_IP" ]; then
              echo "Ingress LoadBalancer IP: $INGRESS_IP"
              break
            fi
            echo "Waiting for IP assignment... ($i/60)"
            sleep 5
          done

          # Update APIM backends with ingress IP
          if [ ! -z "$INGRESS_IP" ]; then
            echo "Updating APIM backend URLs with ingress IP: $INGRESS_IP"
            az apim api update \
              --resource-group $(DEV_RESOURCE_GROUP) \
              --service-name apim-capstone-dev-a1b2c \
              --api-id backend-a-api \
              --service-url "http://$INGRESS_IP"
            az apim api update \
              --resource-group $(DEV_RESOURCE_GROUP) \
              --service-name apim-capstone-dev-a1b2c \
              --api-id backend-b-api \
              --service-url "http://$INGRESS_IP"
            echo "APIM backends updated successfully"
          else
            echo "Warning: Could not update APIM backends - no ingress IP found"
          fi

          kubectl apply -k .

          kubectl delete pod -n $(K8S_NAMESPACE) --all

# =========================
# 2) BUILD AND PUSH TO QA
# =========================
- stage: Deploy_QA
  displayName: Deploy to AKS (QA)
  dependsOn: Deploy_Dev
  jobs:
  - deployment: DeployQA
    displayName: Deploy backend-a & backend-b to aks-capstone-qa
    environment: qa-env  # add approvals here if desired
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          # ---------- LOGIN TO ACR ----------
          - task: AzureCLI@2
            displayName: "Azure Login ACR (QA)"
            inputs:
              azureSubscription: $(AZURE_SUBSCRIPTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az acr login --name $(QA_ACR_NAME)

          # ---------- BUILD DOCKER ----------
          - script: |
              docker build -f backend-a/Dockerfile \
                -t $(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(Build.BuildId) backend-a

              docker build -f backend-b/Dockerfile \
                -t $(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(Build.BuildId) backend-b
            displayName: Build Images (QA)

          # ---------- PUSH IMAGES ----------
          - script: |
              docker push $(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG)
              docker push $(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)
            displayName: "Push Images to ACR (QA)"

          # ---------- DEPLOY TO AKS ----------
          - task: AzureCLI@2
            displayName: Deploy to AKS (QA)
            inputs:
              azureSubscription: $(AZURE_SUBSCRIPTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

                az aks get-credentials \
                  --resource-group $(QA_RESOURCE_GROUP) \
                  --name $(QA_AKS_NAME) \
                  --admin \
                  --overwrite-existing

                cd k8s/overlays/qa

                kustomize edit set image backend-a=$(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG) backend-b=$(QA_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)

                kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.9.6/deploy/static/provider/cloud/deploy.yaml

                # Patch ingress controller to use LoadBalancer instead of NodePort
                kubectl patch svc ingress-nginx-controller -n ingress-nginx -p '{"spec":{"type":"LoadBalancer"}}'

                # Wait for LoadBalancer IP to be assigned
                echo "Waiting for ingress LoadBalancer IP..."
                for i in {1..60}; do
                  INGRESS_IP=$(kubectl get svc ingress-nginx-controller -n ingress-nginx -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                  if [ ! -z "$INGRESS_IP" ]; then
                    echo "Ingress LoadBalancer IP: $INGRESS_IP"
                    break
                  fi
                  echo "Waiting for IP assignment... ($i/60)"
                  sleep 5
                done

                # Update APIM backends with ingress IP
                if [ ! -z "$INGRESS_IP" ]; then
                  echo "Updating APIM backend URLs with ingress IP: $INGRESS_IP"
                  az apim api update \
                    --resource-group $(QA_RESOURCE_GROUP) \
                    --service-name apim-capstone-qa-a1b2c \
                    --api-id backend-a-api \
                    --service-url "http://$INGRESS_IP"
                  az apim api update \
                    --resource-group $(QA_RESOURCE_GROUP) \
                    --service-name apim-capstone-qa-a1b2c \
                    --api-id backend-b-api \
                    --service-url "http://$INGRESS_IP"
                  echo "APIM backends updated successfully"
                else
                  echo "Warning: Could not update APIM backends - no ingress IP found"
                fi

                kubectl apply -k .

                kubectl delete pod -n $(K8S_NAMESPACE) --all

# =========================
# 3) BUILD AND PUSH TO PROD
# =========================
- stage: Deploy_Prod
  displayName: Deploy to AKS (PROD)
  dependsOn: Deploy_QA
  jobs:
  - deployment: DeployProd
    displayName: Deploy backend-a & backend-b to aks-capstone-prod
    environment: production-env
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          # ---------- LOGIN TO ACR ----------
          - task: AzureCLI@2
            displayName: "Azure Login ACR (PROD)"
            inputs:
              azureSubscription: $(AZURE_SUBSCRIPTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az acr login --name $(PROD_ACR_NAME)

          # ---------- BUILD DOCKER ----------
          - script: |
              docker build -f backend-a/Dockerfile \
                -t $(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(Build.BuildId) backend-a

              docker build -f backend-b/Dockerfile \
                -t $(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(Build.BuildId) backend-b
            displayName: Build Images (PROD)

          # ---------- PUSH IMAGES ----------
          - script: |
              docker push $(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG)
              docker push $(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)
            displayName: "Push Images to ACR (PROD)"

          # ---------- DEPLOY TO AKS ----------
          - task: AzureCLI@2
            displayName: Deploy to AKS (PROD)
            inputs:
              azureSubscription: $(AZURE_SUBSCRIPTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

                az aks get-credentials \
                  --resource-group $(PROD_RESOURCE_GROUP) \
                  --name $(PROD_AKS_NAME) \
                  --admin \
                  --overwrite-existing

                cd k8s/overlays/prod

                kustomize edit set image backend-a=$(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-a:$(IMAGE_TAG) backend-b=$(PROD_ACR_NAME)$(ACR_LOGIN_SERVER_SUFFIX)/backend-b:$(IMAGE_TAG)

                kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.9.6/deploy/static/provider/cloud/deploy.yaml

                # Patch ingress controller to use LoadBalancer instead of NodePort
                kubectl patch svc ingress-nginx-controller -n ingress-nginx -p '{"spec":{"type":"LoadBalancer"}}'

                # Wait for LoadBalancer IP to be assigned
                echo "Waiting for ingress LoadBalancer IP..."
                for i in {1..60}; do
                  INGRESS_IP=$(kubectl get svc ingress-nginx-controller -n ingress-nginx -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                  if [ ! -z "$INGRESS_IP" ]; then
                    echo "Ingress LoadBalancer IP: $INGRESS_IP"
                    break
                  fi
                  echo "Waiting for IP assignment... ($i/60)"
                  sleep 5
                done

                # Update APIM backends with ingress IP
                if [ ! -z "$INGRESS_IP" ]; then
                  echo "Updating APIM backend URLs with ingress IP: $INGRESS_IP"
                  az apim api update \
                    --resource-group $(PROD_RESOURCE_GROUP) \
                    --service-name apim-capstone-prod-a1b2c \
                    --api-id backend-a-api \
                    --service-url "http://$INGRESS_IP"
                  az apim api update \
                    --resource-group $(PROD_RESOURCE_GROUP) \
                    --service-name apim-capstone-prod-a1b2c \
                    --api-id backend-b-api \
                    --service-url "http://$INGRESS_IP"
                  echo "APIM backends updated successfully"
                else
                  echo "Warning: Could not update APIM backends - no ingress IP found"
                fi

                kubectl apply -k .

                kubectl delete pod -n $(K8S_NAMESPACE) --all